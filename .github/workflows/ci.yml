name: CI

on:
  workflow_dispatch:
  push:
    # branches:
    #   - main
    #   - feature/*

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        verion: [3.13, 3.12, 3.11, 3.10, 3.9]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.version }}
      - name: Run Python tests
        run: python -m unittest discover test
